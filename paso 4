# Proyecto de ejemplo: Sistema Bancario en Python

Este proyecto demuestra el uso de **constructores (__init__)** y **destructores (__del__)** en clases de Python, organizado con arquitectura de carpetas.

## Estructura
- `modelos/`: contiene las clases (ej. `CuentaBancaria`).
- `servicios/`: contiene la lógica del sistema (ej. `GestorCuentas`).
- `main.py`: punto de entrada del programa.

## Ejecución
```bash
python main.py
